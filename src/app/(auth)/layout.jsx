import React from 'react';

export const metadata = {
    title: "Authentication | PlanWAB - Event Planning and Marketplace",
    description: "Transform your events with premium vendors and intelligent planning tools.",
};

export default function AuthLayout({children,}) {
    return <>{children}</>;
}